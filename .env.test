# Test Environment Variables
# Copy this to .env.test.local and fill in your Plaid SANDBOX credentials

# Plaid Sandbox Credentials (same as .env.local)
PLAID_CLIENT_ID=your_client_id
PLAID_SECRET=your_sandbox_secret
PLAID_ENV=sandbox

# Database (use a separate test database!)
DATABASE_URL=your_test_database_url

# Clerk (can use same as dev for testing)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_key
CLERK_SECRET_KEY=your_secret

# Supabase Vault (test instance or same as dev)
SUPABASE_URL=your_url
SUPABASE_SERVICE_ROLE_KEY=your_key

# Feature flags for testing
ENABLE_REAL_PLAID_TESTS=false  # Set to 'true' to run integration tests with real API
